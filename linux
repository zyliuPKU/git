1. mkdir filename 新建文件夹
2. cd filename 进入文件夹
   cd .. 返回上一级
   cd ~ 返回主目录
   cd 返回主目录
3. rm filename 删除文件
   rm -r filename 删除文件夹
4. cp filename path 把文件复制到path
   cp -r filename path 复制文件夹
5. mv filename path 把文件移动到path
   mv -r filename path 移动文件夹
   mv * path 把当前目录所有文件和文件夹移动到path
6. ls 列出所有文件和文件夹
7. python3 test.py 运行python文件
   taskset -c 0,1 python3 test.py 指定CPU运行python文件
8. vi file 查看文件 
   Esc命令模式，
   :w 保存 
   :q!不保存退出 
   :wq 保存退出 
   :%s/x/y/g 全局x改成y
   i插入模式，直接修改

